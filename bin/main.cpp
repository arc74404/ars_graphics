#include "sources/window/window.hpp"

// void
// someUpdate(UpdatableBuffer& upd_buffer)
// {
// }

int
main()
{
    std::cout << "hello world\n";
    // StableBuffer buffer;

    // for (size_t i = 0; i < 5; ++i)
    // {
    //     buffer.push();
    // }

    // Scene scene;
    // scene.fill(buffer);

    // UpdatableBuffer upd_buffer;
    // scene.bind(&upd_buffer);

    // Window window;

    // while (true)
    // {

    //     someUpdate(upd_buffer);

    //     window.draw(scene);
    // }

    return 0;
}
